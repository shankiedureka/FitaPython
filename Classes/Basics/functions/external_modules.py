#external modules ---> installing the modules from library
#and using importing them

#store them in python library
#install the package from library(pip install <package_name>)
#Python library(cloud) ---> install the package in my local
import pandas as pd
import numpy as np